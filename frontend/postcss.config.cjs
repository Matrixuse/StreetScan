// postcss.config.cjs
module.exports = {
  plugins: {
    // Tailwind CSS processes the @tailwind directives in your CSS file
    tailwindcss: {},
    // Autoprefixer adds vendor prefixes for cross-browser compatibility
    autoprefixer: {},
  },
}
